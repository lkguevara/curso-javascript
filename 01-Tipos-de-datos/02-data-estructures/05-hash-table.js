// * Definición
/*La función hash en Javascript es cualquier función que toma la entrada como datos de tamaño arbitrario y produce una salida como datos de tamaño fijo. Normalmente, el valor devuelto por la función hash se denomina código hash, hash o valor hash*/

function func(string)
{
    //set variable hash as 0
    var hash = 0;
    return hash;
}

console.log(func("hello"));